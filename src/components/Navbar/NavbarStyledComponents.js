/*  This only modifies the navbar background as specified in the changes.  A complete website implementation requires more code. */

const Navbar = styled.nav`
  padding: 1rem 2rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(17, 25, 40, 0.83);
  backdrop-filter: blur(4px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.125);


`;